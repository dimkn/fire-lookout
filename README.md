# fire-lookout

A local **status page** that aggregates status updates from remote sources via their
RSS/Atom feeds. Single repo, three pillars: **backend** (Go), **frontend** (Vue + TS),
and **infrastructure** (a single Docker container serving both).

> **Working on this project (human or AI agent)?**
> Read [`AGENTS.md`](AGENTS.md) first. It is the single source of truth for the
> architecture, conventions, commands, and workflow. Keep it up to date as decisions land.

Current version: see [`VERSION`](VERSION).
